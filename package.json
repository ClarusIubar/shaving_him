{
  "name": "shaving-him",
  "version": "1.0.6",
  "private": true,
  "type": "module",
  "description": "Browser-based ASCII shaving game with an in-browser 1-photo processing pipeline.",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage --test-coverage-exclude=tests/** --test-coverage-lines=100 --test-coverage-functions=100 --test-coverage-branches=90"
  }
}
