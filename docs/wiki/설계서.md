# 📁 [System Architecture & Design] Shaving Him 설계서 - v1.14.0

> **위키 퀵 내비게이션**: [🏠 Wiki 홈](Home) | [🎮 라이브 게임 플레이](https://clarusiubar.github.io/shaving_him/) | [📋 기획서 (PRD)](기획서) | [📁 시스템 설계서](설계서) | [🏛️ ADR 명세서](ADR) | [📂 소스모듈 명세서](프로젝트_디렉토리_및_모듈_구조_명세서) | [⚡ 세부 기술 명세서](명세서) | [📊 수식/퍼징 검증서](스코어링_이론계산_실측값_검증) | [🧪 테스트 체계서](테스트_체계_및_TDD_명세서) | [📜 변경 이력](CHANGELOG)

---

## 📌 문서 목차 (Table of Contents)
1. [전체 시스템 아키텍처 개요 (System Architecture Overview)](#1-전체-시스템-아키텍처-개요-system-architecture-overview)
2. [SOLID 5대 원칙 적용 현황 및 잔여 설계 과제 (SOLID Assessment & Technical Debt)](#2-solid-5대-원칙-적용-현황-및-잔여-설계-과제-solid-assessment--technical-debt)
3. [거시적 시스템 아키텍처 조감도 (Macro System Blueprint)](#3-거시적-시스템-아키텍처-조감도-macro-system-blueprint)
4. [5계층별 세부 아키텍처 설계 (5-Layer Architectural Design)](#4-5계층별-세부-아키텍처-설계-5-layer-architectural-design)
   - [4.1. Domain Core Layer (순수 비즈니스 로직)](#41-domain-core-layer-순수-비즈니스-로직)
   - [4.2. Ports Layer (DIP 추상 계약)](#42-ports-layer-dip-추상-계약)
   - [4.3. Application Layer (유스케이스 오케스트레이션)](#43-application-layer-유스케이스-오케스트레이션)
   - [4.4. Adapters Layer (인프라 & 캔버스 구현체)](#44-adapters-layer-인프라--캔버스-구현체)
   - [4.5. Interface UI Layer (수동적 뷰 & 입력 제어기)](#45-interface-ui-layer-수동적-뷰--입력-제어기)
5. [데이터 수명주기 및 상호작용 시퀀스 다이어그램 (Sequence Diagrams)](#5-데이터-수명주기-및-상호작용-시퀀스-다이어그램-sequence-diagrams)
   - [5.1. 앱 부트스트랩 및 DI 주입 시퀀스](#51-앱-부트스트랩-및-di-주입-시퀀스)
   - [5.2. 1-Photo 아스키 파이프라인 생성 시퀀스](#52-1-photo-아스키-파이프라인-생성-시퀀스)
   - [5.3. 마우스 드래그 면도 및 실시간 사운드/파티클 피드백 시퀀스](#53-마우스-드래그-면도-및-실시간-사운드파티클-피드백-시퀀스)
   - [5.4. 올클리어 승리 및 리스타트 시퀀스](#54-올클리어-승리-및-리스타트-시퀀스)

---

## 1. 전체 시스템 아키텍처 개요 (System Architecture Overview)

본 시스템은 도메인 모델이 브라우저 DOM API나 특정 UI 렌더러에 종속되지 않도록 **5계층 클린 아키텍처(5-Layer Clean Architecture & Hexagonal Ports/Adapters)** 패턴을 도입하여 분리되어 있습니다.

```text
[UI Presentation Layer] (InputManager, BrushController, CursorView, CanvasRenderer, HUD, Views, SoundEffects)
          │
          ▼
[Application Layer]     (CompositionRoot, GameOrchestrator, StagePipeline)
     │          │
     ▼          ▼
[Domain Core] [Abstract Ports] ◄────── [Secondary Adapters]
(HairGrid,     (StageSource,           (StaticJsonStage,
 ShaveSession,  ImageProcessor,         CanvasImageProcessor,
 ScoreCalc,     AsciiConverter,         CanvasAsciiConverter,
 SessionTimer,  DiffEngine)             DeltaDiffEngine,
 GridGeom,                              ImageFileLoader)
 LineRaster)
```

- **의존성 역전 원칙(DIP)**: 상위 계층(App)은 하위 구현체(Adapters)를 직접 import하지 않으며, 오직 `ports/`에 정의된 추상 인터페이스 계약에만 의존합니다.
- **Composition Root**: `src/app/composition-root.js`가 단일 진입점에서 모든 어댑터와 도메인/앱 인스턴스를 생성하고 생성자 주입(Constructor Injection)을 통해 결합합니다.

---

## 2. SOLID 5대 원칙 적용 현황 및 설계 달성도 (SOLID Assessment)

| 원칙 | 적용된 구조적 개선점 및 설계 패턴 | v1.14.0 리팩토링 달성 내역 | 설계 품질 수준 |
| :--- | :--- | :--- | :---: |
| **SRP (단일 책임)** | `InputManager`(이벤트), `ImageFileLoader`(파일), `LineRasterizer`(기하), `CursorView`(커서 DOM), `SessionTimer`(클록) 분리 | `CursorView` 분리로 `BrushController`의 DOM 조작 배제, `SessionTimer`로 타이머 제어 분리 | **100% 달성** |
| **OCP (개방-폐쇄)** | `StageSourceRegistry` 핸들러 맵, `IScoringStrategy` 전략 패턴 도입 | `DefaultScoringStrategy` 추출 및 `ScoreCalculator` 전략 주입 패턴 확립 | **100% 달성** |
| **LSP (리스코프 치환)** | 모든 어댑터(`StaticJsonStageAdapter` 포함)가 `StageSourcePort` 상속 및 계약 충족 | `StaticJsonStageAdapter extends StageSourcePort` 및 `canHandle` 정합 | **100% 달성** |
| **ISP (인터페이스 분리)** | `HairGrid.toReadOnlyView()`, `InputManager`의 세분화된 뷰(`statsView`, `modalView`) 주입 | `InputManager`가 거대 `HUD` 대신 최소 필요 뷰만 주입받도록 결합도 완화 | **100% 달성** |
| **DIP (의존성 역전)** | `StagePipeline`, `CanvasRenderer`, `GameOrchestrator`의 생성자 의존성 주입 지원 | `CompositionRoot` 단일 조립 루트에서 모든 컴포넌트 생성자 주입 완비 | **100% 달성** |

---

## 3. 거시적 시스템 아키텍처 조감도 (Macro System Blueprint)

```mermaid
flowchart TD
  subgraph UILayer ["Interface & Presentation Layer (src/ui)"]
    Input["InputManager (Command Dispatcher)"]
    Brush["BrushController (Pure Input Coordinates)"]
    Cursor["CursorView (GPU transform translate3d)"]
    Renderer["CanvasRenderer (Dirty Batching)"]
    Particle["ParticleSystem (Autonomous Loop)"]
    Sound["SoundEffects (Web Audio API)"]
    HUD["HUD (Unified UI Facade)"]
    Views["StatsHUDView / StageSelectModalView / LoadingOverlayView / GameOverOverlayView"]
  end

  subgraph AppLayer ["Application Layer (src/app)"]
    CompRoot["Composition Root (DI Container)"]
    Orchestrator["GameOrchestrator (Pure State Mediator)"]
    Timer["SessionTimer (Game Clock Service)"]
    Pipeline["StagePipeline (4-Stage Pipeline)"]
    Handlers["StageSourceHandlers (Registry Strategy)"]
  end

  subgraph PortsLayer ["Abstract Ports Layer (src/ports)"]
    StagePort["StageSourcePort"]
    ImagePort["ImageProcessorPort"]
    AsciiPort["AsciiConverterPort"]
    DiffPort["DiffEnginePort"]
  end

  subgraph DomainLayer ["Pure Domain Core (src/domain)"]
    Session["ShaveSession (State Machine)"]
    Grid["HairGrid (1D Uint8Array 0B)"]
    Score["ScoreCalculator (Strategy-based)"]
    ScoreStrat["IScoringStrategy (Default / Hardcore)"]
    Geom["GridGeometry (Immutable Value Object)"]
    Raster["LineRasterizer (Bresenham 8-Connected)"]
    Validator["SchemaValidator (Runtime Invariant Defense)"]
    Policy["GamePolicy (Victory Rules)"]
  end

  subgraph TypesLayer ["Specification Types (src/types)"]
    TypeDefs["index.d.ts (DTO & Port Contracts)"]
  end

  subgraph AdaptersLayer ["Secondary Driven Adapters (src/adapters)"]
    JsonAdap["StaticJsonStageAdapter (Contract-Compliant)"]
    ImgAdap["CanvasImageProcessorAdapter"]
    FileLoader["ImageFileLoader"]
    AsciiAdap["CanvasAsciiConverterAdapter"]
    DiffAdap["DeltaDiffEngineAdapter"]
  end

  Input --> Orchestrator
  Input --> Brush
  Input --> HUD
  Input --> Sound
  Brush --> Cursor
  Brush --> Raster
  HUD --> Views
  Renderer --> Particle

  CompRoot --> Orchestrator
  CompRoot --> Timer
  CompRoot --> Pipeline
  CompRoot --> Geom
  CompRoot --> Policy

  Orchestrator --> Timer
  Orchestrator --> Session
  Session --> Grid
  Session --> Score
  Score --> ScoreStrat
  Session --> Validator

  Pipeline --> Handlers
  Handlers --> StagePort
  Pipeline --> ImagePort
  Pipeline --> AsciiPort
  Pipeline --> DiffPort

  ImgAdap -.-> ImagePort
  ImgAdap --> FileLoader
  AsciiAdap -.-> AsciiPort
  DiffAdap -.-> DiffPort
  JsonAdap -.-> StagePort
```

---

## 4. 5계층별 세부 아키텍처 설계 (5-Layer Architectural Design)

### 4.1. Domain Core Layer (`src/domain/`)
- **`HairGrid`**: 1D `Uint8Array` 기반의 비트맵 매트릭스로, $r \times \text{cols} + c$ 단일 인덱스를 통해 O(1) 비트 조회/반전을 수행하며 가비지 컬렉션(GC) 할당을 발생시키지 않습니다.
- **`ShaveSession`**: `INIT`, `RUNNING`, `PAUSED`, `WON`, `TIMEOUT` 5대 세션 상태 머신을 통제하고 타이머 틱 및 스냅샷 조회를 관장합니다.
- **`ScoreCalculator`**: 연속 콤보 스트릭($k$)에 따른 누적 점수($10 \times \min(k, 10)$), 잔여 시간 보너스, 올클리어 보너스를 계산합니다.
- **`GridGeometry`**: 불변(Immutable) 값 객체로 캔버스 뷰포트 $x, y$ 좌표를 안전하게 그리드 셀 $(r, c)$로 변환하는 정규화 매핑을 전담합니다.
- **`LineRasterizer`**: 초고속 마우스 드래그 시 발생하는 좌표 간격을 브레젠험 8-연결 알고리즘으로 보간하여 끊김 없는 면도를 보장합니다.
- **`SchemaValidator`**: `validateStageData` 및 `validateSnapshot`을 통해 DTO 런타임 데이터 구조를 방어합니다.

### 4.2. Ports Layer (`src/ports/`)
- **`StageSourcePort`**: `loadStage(source, targetCols, targetRows, options, onProgress)` 추상 계약.
- **`ImageProcessorPort`**: `processImageSource(source, targetWidth, targetHeight)` 추상 계약.
- **`AsciiConverterPort`**: `convertToAsciiGrid(imageData, targetCols, targetRows)` 추상 계약.
- **`DiffEnginePort`**: `extractHairMask(imageData, targetCols, targetRows, skinLumThreshold)` 추상 계약.

### 4.3. Application Layer (`src/app/`)
- **`CompositionRoot`**: 어댑터와 도메인 객체를 생성자 주입 방식으로 배선하여 결합도를 역전시키는 조립 루트.
- **`GameOrchestrator`**: `setInterval` 1초 클록을 관리하며, 상태 변경 시 단일 DTO 이벤트(`snapshot`, `stageData`, `hairView`, `dirtyCells`)를 UI로 통지.
- **`StagePipeline`**: 이미지 리사이징 ➔ 그레이스케일 ➔ 아스키 텍스트 ➔ 델타 마스킹의 4단계를 프로그레스 콜백과 함께 수행.

### 4.4. Adapters Layer (`src/adapters/`)
- **`CanvasImageProcessorAdapter`**: 브라우저 캔버스를 사용하여 `HTMLImageElement` 또는 `File`을 리사이징하고 픽셀 데이터 추출. 비동기 파일 처리는 `ImageFileLoader`로 위임.
- **`CanvasAsciiConverterAdapter`**: 픽셀 휘도 값을 10단계 문자 램프에 매핑하여 아스키 매트릭스 생성.
- **`DeltaDiffEngineAdapter`**: 피부톤 휘도 임계값을 초과하는 어두운 수염 픽셀을 검출하여 수염 비트맵 좌표 분리.
- **`StaticJsonStageAdapter`**: JSON 정적 데이터 로드 및 브라우저 로컬 `window.EMBEDDED_GAME_DATA` 폴백 지원.

### 4.5. Interface UI Layer (`src/ui/`)
- **`InputManager`**: 키보드 단축키(`1~4`, `R`), 마우스/터치 브러시 버튼, 사운드 토글, 모달 창 이벤트 등록 및 `destroy()` 정리 전담.
- **`BrushController`**: 캔버스 포인터 이벤트 수신, 브러시 반경 클램핑, GPU 하드웨어 가속(`translate3d`) 커서 애니메이션 처리.
- **`CanvasRenderer`**: Dirty Cell 기반 부분 다시 그리기 및 `ParticleSystem` 비산 파티클 배치 렌더링.
- **`SoundEffects`**: Web Audio API 기반 100% 절차적 오디오 합성 엔진.

---

## 5. 데이터 수명주기 및 상호작용 시퀀스 다이어그램 (Sequence Diagrams)

### 5.1. 앱 부트스트랩 및 DI 주입 시퀀스
```mermaid
sequenceDiagram
    autonumber
    actor User
    participant Main as main.js
    participant Root as CompositionRoot
    participant Orch as GameOrchestrator
    participant Input as InputManager
    participant HUD as HUD (Facade)

    User->>Main: 페이지 로드 (DOMContentLoaded)
    Main->>Root: createCompositionRoot(customAdapters)
    Root-->>Main: { orchestrator, geometry, gamePolicy }
    Main->>HUD: new HUD(gamePolicy, document)
    Main->>Input: new InputManager({ doc, win, brushController, orchestrator, hud, sound })
    Input->>Input: bindEvents() (키보드 1-4/R, 마우스, 사운드 토글 등록)
    Main->>HUD: showStartModal() (시작 모달 표출)
```

### 5.2. 1-Photo 아스키 파이프라인 생성 시퀀스
```mermaid
sequenceDiagram
    autonumber
    actor User
    participant Modal as StageSelectModalView
    participant Main as main.js
    participant Pipe as StagePipeline
    participant ImgAdap as CanvasImageProcessorAdapter
    participant Loader as ImageFileLoader
    participant AsciiAdap as CanvasAsciiConverterAdapter
    participant DiffAdap as DeltaDiffEngineAdapter
    participant Orch as GameOrchestrator

    User->>Modal: 사진 드롭 / 선택
    Modal->>Main: startStageWithSource(file)
    Main->>Orch: loadAndStartStage(file)
    Orch->>Pipe: loadStage(file)
    Pipe->>ImgAdap: processImageSource(file)
    ImgAdap->>Loader: loadImageFile(file)
    Loader-->>ImgAdap: HTMLImageElement
    ImgAdap-->>Pipe: ImageData (280x219)
    Pipe->>AsciiAdap: convertToAsciiGrid(ImageData)
    AsciiAdap-->>Pipe: textGrid
    Pipe->>DiffAdap: extractHairMask(ImageData)
    DiffAdap-->>Pipe: { hairPositions, totalHairCount }
    Pipe-->>Orch: StageDataDTO
    Orch->>Orch: new ShaveSession(StageDataDTO)
    Orch->>Main: notifyUpdate(null, false)
    Main->>Modal: hideStartModal()
```

### 5.3. 마우스 드래그 면도 및 실시간 사운드/파티클 피드백 시퀀스
```mermaid
sequenceDiagram
    autonumber
    actor User
    participant Canvas as Canvas Element
    participant Brush as BrushController
    participant Raster as LineRasterizer
    participant Orch as GameOrchestrator
    participant Session as ShaveSession
    participant Grid as HairGrid
    participant Sound as SoundEffects
    participant Renderer as CanvasRenderer

    User->>Canvas: mousedown (r0, c0) & mousemove (r1, c1)
    Canvas->>Brush: handlePointerMove(clientX, clientY)
    Brush->>Raster: rasterizeLine(r0, c0, r1, c1)
    Raster-->>Brush: [{r0,c0}, ..., {r1,c1}]
    loop 각 보간 좌표마다
        Brush->>Orch: shave(r, c, radius)
        Orch->>Session: shave(r, c, radius)
        Session->>Grid: shave(r, c, radius)
        Grid-->>Session: { count, dirtyCells }
        Session->>Session: scoreCalculator.addShave(count)
        Session-->>Orch: { removed, dirtyCells }
        alt removed > 0 (수염 깎임)
            Orch->>Sound: playShaveSound()
            Orch->>Renderer: requestRender(stageData, hairView, dirtyCells)
            Renderer->>Renderer: spawnParticles(dirtyCells)
        end
    end
```

### 5.4. 올클리어 승리 및 리스타트 시퀀스
```mermaid
sequenceDiagram
    autonumber
    participant Session as ShaveSession
    participant Grid as HairGrid
    participant Orch as GameOrchestrator
    participant Sound as SoundEffects
    participant HUD as HUD (Facade)
    actor User

    Session->>Grid: shave() ➔ remainingHairs == 0
    Grid-->>Session: count
    Session->>Session: status = SessionStatus.WON
    Session-->>Orch: session status WON
    Orch->>Orch: stopTimer()
    Orch->>Sound: playWinSound() (C5-E5-G5-C6 팡파레)
    Orch->>HUD: showGameOver(snapshot, onRestart)
    HUD->>HUD: GameOverOverlayView 표출 (최종 점수, 시간 보너스, 올클리어 보너스)
    User->>HUD: 'R' 키보드 단축키 또는 재시작 버튼 클릭
    HUD->>Orch: restart()
    Orch->>Session: initStage(currentStageData) & start()
    Orch->>HUD: hideOverlay()
```
