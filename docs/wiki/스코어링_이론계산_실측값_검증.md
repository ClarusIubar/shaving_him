# 📊 [Math & Fuzzing Verification] Shaving Him 수식 및 퍼징 검증서 - v1.14.0

> **위키 퀵 내비게이션**: [🏠 Wiki 홈](Home) | [🎮 라이브 게임 플레이](https://clarusiubar.github.io/shaving_him/) | [📋 기획서 (PRD)](기획서) | [📁 시스템 설계서](설계서) | [🏛️ ADR 명세서](ADR) | [📂 소스모듈 명세서](프로젝트_디렉토리_및_모듈_구조_명세서) | [⚡ 세부 기술 명세서](명세서) | [📊 수식/퍼징 검증서](스코어링_이론계산_실측값_검증) | [🧪 테스트 체계서](테스트_체계_및_TDD_명세서) | [📜 변경 이력](CHANGELOG)

---

## 📌 문서 목차 (Table of Contents)
1. [점수 산정 수학 공식 및 콤보 가산 모델](#1-점수-산정-수학-공식-및-콤보-가산-모델)
2. [이론 계산치 vs 단위 테스트 실측치 검증 매트릭스](#2-이론-계산치-vs-단위-테스트-실측치-검증-매트릭스)
3. [1,000회 무작위 브레젠험 Fuzzing 기하 불변성 증명](#3-1000회-무작위-브레젠험-fuzzing-기하-불변성-증명)
4. [500회 뷰포트 스케일링 좌표 변환 퍼징 검증](#4-500회-뷰포트-스케일링-좌표-변환-퍼징-검증)

---

## 1. 점수 산정 수학 공식 및 콤보 가산 모델

### 1.1. 기본 면도 점수 및 콤보 배율 공식 (`DefaultScoringStrategy`)
수염 $N$개를 연속해서 깎을 때의 기본 획득 점수($S_{\text{base}}$)는 누적 연속 스트릭($\text{streak}$)에 따라 배율($M$)이 5단계로 증가합니다:

$$M(\text{streak}) = \min\left(\left\lfloor \frac{\text{streak}}{5} \right\rfloor + 1, 5\right)$$

- $\text{streak} = 0 \sim 4$: $1$배 (수염 1개당 $+1$점)
- $\text{streak} = 5 \sim 9$: $2$배 (수염 1개당 $+2$점)
- $\text{streak} = 10 \sim 14$: $3$배 (수염 1개당 $+3$점)
- $\text{streak} = 15 \sim 19$: $4$배 (수염 1개당 $+4$점)
- $\text{streak} \ge 20$: $5$배 (상한선 고정, 수염 1개당 $+5$점)

### 1.2. 시간 보너스 (Time Bonus)
$$S_{\text{time}} = \max(0, T_{\text{left}}) \times 5$$

### 1.3. 올클리어 보너스 (All-Clear Bonus)
$$S_{\text{allclear}} = \begin{cases} 500 & (\text{remainingHairs} = 0) \\ 0 & (\text{remainingHairs} > 0) \end{cases}$$

### 1.4. 최종 점수 (Total Score)
$$S_{\text{total}} = S_{\text{base}} + S_{\text{time}} + S_{\text{allclear}}$$

---

## 2. 이론 계산치 vs 단위 테스트 실측치 검증 매트릭스

| 시나리오 | 깎은 수염 수 ($N$) | 스트릭 ($\text{streak}$) | 잔여 시간 ($T_{\text{left}}$) | 잔여 수염 | 이론 계산 점수 ($S_{\text{total}}$) | 테스트 실측값 | 일치 여부 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **단일 수염 1회 깎기** | $1$ | $1$ | $55$초 | $10$개 | $1 \times 1 + (55 \times 5) + 0 = \mathbf{276}$ | $276$ | ✅ 100% 일치 |
| **5콤보 달성 (2배 진입)** | $5$ | $5$ | $50$초 | $5$개 | $5 \times 2 + (50 \times 5) + 0 = \mathbf{260}$ | $260$ | ✅ 100% 일치 |
| **20콤보 상한 도달 (5배)** | $20$ | $20$ | $45$초 | $20$개 | $20 \times 5 + (45 \times 5) + 0 = \mathbf{325}$ | $325$ | ✅ 100% 일치 |
| **올클리어 승리** | $50$ | $50$ | $30$초 | $0$개 | $S_{\text{base}} + (30 \times 5) + 500$ | 완전 일치 | ✅ 100% 일치 |
| **빈 셀 헛깎기 시 스트릭 리셋** | $5 \rightarrow 0 \rightarrow 1$ | $0 \rightarrow 1$ | $40$초 | $10$개 | $10 \rightarrow 10 + 1 \times 1 = \mathbf{11}$ | $11$ | ✅ 100% 일치 |

---

## 3. 1,000회 무작위 브레젠험 Fuzzing 기하 불변성 증명

[`tests/domain/grid-geometry-fuzzing.test.js`](file:///d:/Code305/shaving_him/tests/domain/grid-geometry-fuzzing.test.js)에서 $-100 \sim 100$ 범위의 무작위 난수 좌표를 주입하여 1,000회의 선분 생성 불변성을 증명하였습니다.

```javascript
test('Fuzzing - rasterizeLine mathematical invariant verification (1000 random line vectors)', () => {
    // 1,000개 무작위 선분 벡터 검증
    for (let i = 0; i < 1000; i++) {
        const points = rasterizeLine(r0, c0, r1, c1);

        // 불변식 1: 배열이 비어있지 않음
        assert.ok(points.length > 0);

        // 불변식 2: 시작점과 끝점의 완전 일치
        assert.equal(points[0].r, r0);
        assert.equal(points[0].c, c0);
        assert.equal(points[points.length - 1].r, r1);
        assert.equal(points[points.length - 1].c, c1);

        // 불변식 3: 체비쇼프 거리 기반 점의 개수 일치
        const expectedLength = Math.max(Math.abs(r1 - r0), Math.abs(c1 - c0)) + 1;
        assert.equal(points.length, expectedLength);

        // 불변식 4: 8-방향 인접성 연속성 유지 (구멍 없음)
        for (let j = 0; j < points.length - 1; j++) {
            const dr = Math.abs(points[j + 1].r - points[j].r);
            const dc = Math.abs(points[j + 1].c - points[j].c);
            assert.equal(Math.max(dr, dc), 1);
        }
    }
});
```
- **검증 결과**: **1,000회 시도 중 1,000회 통과 (결함률 0.00%)**.

---

## 4. 500회 뷰포트 스케일링 좌표 변환 퍼징 검증

- **테스트 환경**: 임의의 마우스 클릭 좌표($-500 \sim 2500$px), 캔버스 크기($100 \sim 1920$px), 뷰포트 오프셋($0 \sim 500$px) 500회 무작위 대입.
- **불변식 보증**:
  1. `result.row`와 `result.col`은 절대 `NaN`이나 무한대(`Infinity`)를 반환하지 않고 유효 정수 반환.
  2. 바운딩 박스 내부 좌표는 항상 `[0, cols - 1]`, `[0, rows - 1]` 범위 내로 안전하게 클램핑/매핑.
  3. 바운딩 박스 외부 좌표는 정확히 `row: -1, col: -1` 에러 코드를 반환하여 안전 차단.
- **검증 결과**: **500회 시도 중 500회 통과 (오차율 0.00%)**.
